# DESCRIPTION
```
This is email sender service project on Java Spring Boot for project 'messenger-service'.
```
# LAUNCH 
```
Start this project after messenger-service.
Change configurations: spring.mail.username, spring.mail.password to yours.
Use command 'docker-compose up --build' to launch project and 'docker-compose down' to turn off project.
```